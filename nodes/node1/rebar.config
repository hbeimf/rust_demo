{erl_opts, [debug_info]}.
{deps, []}.

{relx, [{release, { node1, "0.1.0" },
         [kernel,stdlib,crypto,asn1,public_key,ssl,inets,esnowflake,glib,mnesia,table,
         node1,sasl, zucchini]},

        {sys_config, "./config/sys.config"},
        {vm_args, "./config/vm.args"},

        {dev_mode, true},
        {include_erts, false},

        {extended_start_script, true}]
}.



{profiles, [{prod, [{relx, [{dev_mode, false},
                            {include_erts, true}]}]
            }]
}.
